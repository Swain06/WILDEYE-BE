// Type declaration for leaflet.heat plugin
// The plugin attaches L.heatLayer to the Leaflet namespace at runtime.
declare module 'leaflet.heat' {
    // The plugin self-registers on L; no named exports needed.
    const _: unknown;
    export default _;
}
